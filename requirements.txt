# Core PoI red-teaming pipeline
pynacl>=1.5          # Ed25519 signatures
msgpack>=1.0         # canonical record serialization (vendored prototype)
numpy>=1.22          # determinism runner
requests>=2.30       # simulated node endpoint calls
openai>=1.30         # correctness-and-safety judge
python-dotenv>=1.0   # load judge API key from .env
tabulate>=0.9        # report tables
matplotlib>=3.5      # baseline latency plot (vendored prototype)

# Testing
pytest>=8.0
pytest-asyncio>=1.0

# Planned (not yet used): property-based fuzzing of the validation logic
hypothesis>=6.0
